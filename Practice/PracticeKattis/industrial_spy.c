#include <stdio.h>
#include <string.h>

int main(){

    int n;
    scanf("%d", &n);

    char *array[n];
    char* tmp;

    for(int i = 0; i < n; i++){
        scanf("%s", &tmp);
        array[i] = tmp;
    }

    for(int i = 0; i < n; i++){
        
    }

    return 0;
}